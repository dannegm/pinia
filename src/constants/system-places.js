// Human labels for `system_places.key`. Only 'casa' exists today, but the
// table is designed to grow (e.g. 'trabajo', 'escuela') without a schema
// change — new keys just need an entry here to get a nice label.
export const SYSTEM_PLACE_LABELS = {
    casa: 'Casa',
};
